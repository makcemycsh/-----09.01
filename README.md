# AppMetrica UI — METR-75473

Новый отчет воронок за фичей `FunnelReportAccess` (Доступ к новому отчету воронок).

- С фичей открывается новый UI отчета (промо пустого списка).
- Без фичи сохраняется текущее поведение — BEM-версия отчета.

```bash
npm install
npm test
npm run dev
```

Включить фичу: переключатель в шапке или `?feature=FunnelReportAccess`.
